import SwiftUI

struct ContentView: View {
    var body: some View {
  Text("Lego Library")
            .font(.largeTitle)
            .foregroundColor(.red)
            .padding()
        Spacer()
        Text("Start")
            .foregroundColor(.blue)
            .padding()
        Spacer()
    }
}
